export const selectCartItems = (state) => state.shoppingCart.cartItems;
export const selectError = (state) => state.shoppingCart.error;
export const selectTotalPrice = (state) => state.shoppingCart.totalPrice;
